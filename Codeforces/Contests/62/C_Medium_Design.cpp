#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define forn(i,n) for(ll i=0;i<n;i++)
void solve(){
    ll n, m;
    cin >> n >> m;
    map<ll, ll> mp;
    forn(i,n){
        ll x, y;
        cin >> x >> y;
        mp[x] = y;
    }
}
int main(){
ll t;
cin>>t;
while(t--){
solve();
}
return 0;
}